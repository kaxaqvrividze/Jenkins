pipeline {
    // ...
    stages {
        // ... (Checkout SCM stage)
        stage('Build') {
            steps {
                // Change 'sh' to 'bat' for Windows commands
                bat 'echo Hello, Jenkins on Windows!'
                // Replace 'echo' with your actual build command (e.g., 'mvn clean install', 'npm install')
            }
        }
    }
}
